{% extends 'base.html.twig' %}

{% block body %}

<article>
<h1>{{ article.title }}</h1>
<section>
    <img src="{{ article.image }}" alt="" />
</section>
<section>
    {{ article.content }}
</section>
</article>

{% endblock %}